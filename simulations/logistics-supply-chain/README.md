# Synthetic Logistics and Supply Chain Operation

Synthetic validated executable demonstration for `logistics-supply-chain` using the canonical AXIOM simulation contracts and runner. All external/regulated/production effects are modeled as local deterministic files; no production deployment or compliance claim is made.

Run: `python simulations/logistics-supply-chain/execute.py --out-dir /tmp/logistics-supply-chain-evidence` from `axiom-research` with sibling `axiom-apex` and `axiom-ason` checkouts.
