# Synthetic Specialty Care Administration

Synthetic validated executable demonstration for `specialty-care-administration` using the canonical AXIOM simulation contracts and runner. All external/regulated/production effects are modeled as local deterministic files; no production deployment or compliance claim is made.

Run: `python simulations/specialty-care-administration/execute.py --out-dir /tmp/specialty-care-administration-evidence` from `axiom-research` with sibling `axiom-apex` and `axiom-ason` checkouts.
